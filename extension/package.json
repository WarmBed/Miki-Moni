{
  "name": "miki-helper",
  "displayName": "miki-moni helper",
  "publisher": "miki-moni",
  "version": "0.1.0",
  "description": "Companion VSCode extension for miki-moni daemon — receives prompt-submit commands and dispatches them to the local Claude panel.",
  "repository": {
    "type": "git",
    "url": "https://github.com/miki-moni/miki-moni.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "miki-helper.showStatus",
        "title": "miki-moni helper: Show status"
      }
    ],
    "configuration": {
      "title": "miki-moni helper",
      "properties": {
        "miki-helper.daemonUrl": {
          "type": "string",
          "default": "ws://127.0.0.1:8765/ws_ext",
          "description": "WebSocket URL of the miki-moni daemon"
        },
        "miki-helper.prefillDelayMs": {
          "type": "number",
          "default": 500,
          "description": "Milliseconds to wait after firing the vscode:// URI before pressing Enter"
        }
      }
    }
  },
  "scripts": {
    "compile": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --platform=node --target=node18 --external:vscode --format=cjs --sourcemap",
    "typecheck": "tsc -p . --noEmit",
    "watch": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --platform=node --target=node18 --external:vscode --format=cjs --sourcemap --watch",
    "package": "vsce package --no-dependencies",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.5.12",
    "@vscode/vsce": "^3.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
